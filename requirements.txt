# Podstawowe biblioteki
numpy>=1.19.2          # Bez zmian, wersja 1.19.2 jest kompatybilna
pandas>=1.1.3           # Bez zmian, wersja 1.1.3 jest kompatybilna
scipy>=1.7.1           # Bez zmian, wersja 1.7.1 jest kompatybilna

# PyTorch i powiązane biblioteki
torch==1.13.1+cu117     # Aktualizacja do najnowszej wersji kompatybilnej z Python 3.9
torchvision==0.14.1+cu117 # Dopasowanie do wersji PyTorch
torchaudio==0.13.1+cu117 # Dopasowanie do wersji PyTorch

# Biblioteki do przetwarzania danych i uczenia maszynowego
scikit-learn==1.0.1     # Bez zmian
tslearn==0.5.2          # Bez zmian

# Biblioteki do wizualizacji
matplotlib>=3.3.4       # Bez zmian
seaborn==0.11.2         # Bez zmian

# Biblioteki do obróbki tekstu i NLP
nltk==3.6.7            # Bez zmian
transformers==4.31.0    # Aktualizacja do najnowszej wersji

# Biblioteki do obsługi różnych formatów danych
h5py>=3.1.0            # Bez zmian
jsonpickle==2.0.0       # Bez zmian

# Biblioteki do efektywnego przetwarzania danych
numba==0.56.4           # Aktualizacja do najnowszej wersji

# Biblioteki do wizualizacji modeli
graphviz==0.19.1        # Bez zmian
torchviz==0.0.2         # Bez zmian

# Biblioteki do logowania i monitorowania
tensorboard==2.13.0     # Aktualizacja do najnowszej wersji

# Biblioteki do testowania
pytest==6.2.5          # Bez zmian

# Dodatkowe narzędzia
tqdm==4.65.0       

